name: ROS Build

on:
  push:
    branches: [ master, melodic, noetic, foxy ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: 
      image: ros:noetic-ros-base-focal

    steps:
    - name: Environment Info
      run: |
        pwd
        uname -r
        lsb_release -a

    - name: Install Catkin
      run: |
        apt-get update
        apt-get install -y tree python3-catkin-lint python3-pip
        pip3 install osrf-pycommon
        apt-get install -y python-catkin-tools

    - uses: actions/checkout@v2
      with: 
        path: src/ros_best_practices 

    - name: Rosdep
      run: |
        rosdep update
        rosdep install --from-paths src --ignore-src -r -y

    - name: Build (Release)
      shell: bash
      run: |
        source /opt/ros/noetic/setup.bash
        tree 
        catkin init
        catkin build --cmake-args -DCMAKE_BUILD_TYPE=Release
        
    - name: Test
      shell: bash
      run: |
        source devel/setup.bash
        catkin run_tests
